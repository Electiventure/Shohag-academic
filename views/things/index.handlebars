<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>List of Measurements</title>
</head>

<body>
  <h1>List of Measurements</h1>

  {{#if things.length}}
    <ul>
      {{#each things}}
        <li>
          <strong>{{place}}</strong> - {{date}} - {{value}} - {{type}}
          <a href="/things/{{_id}}">View</a>
          <a href="/things/{{_id}}/edit">Edit</a>
          <form action="/things/{{_id}}?_method=DELETE" method="POST">
            <!-- Include CSRF token if applicable -->
            <input type="hidden" name="_csrf" value="{{csrfToken}}">
            <button type="submit" onclick="return confirm('Are you sure?')">Delete</button>
          </form>
        </li>
      {{/each}}
    </ul>
  {{else}}
    <p>No measurements found.</p>
  {{/if}}

  <!-- Include CSRF token if applicable -->
  <form method="post" action="/things/{{thing._id}}?_method=DELETE">
    <input type="hidden" name="_csrf" value="{{csrfToken}}">
    <button type="submit" onclick="return confirm('Are you sure?')">Delete Thing</button>
  </form>

</body>

</html>
