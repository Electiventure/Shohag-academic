<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Edit Measurement</title>
</head>

<body>
  <h1>Edit Measurement</h1>

  <form action="/things/{{thing._id}}?_method=PUT" method="POST">
    <label for="place">Place:</label>
    <input type="text" id="place" name="thing[place]" value="{{thing.place}}" required>

    <label for="date">Date:</label>
    <input type="text" id="date" name="thing[date]" value="{{thing.date}}" required>

    <label for="value">Value:</label>
    <input type="number" id="value" name="thing[value]" value="{{thing.value}}" step="0.1" required>

    <label for="type">Type:</label>
    <input type="text" id="type" name="thing[type]" value="{{thing.type}}" required>

    <button type="submit">Update Measurement</button>
  </form>

  <!-- Ensure thing._id is defined before generating the link -->
  {{#if thing._id}}
    <!-- Use Express's url function to construct the URL -->
    <a href="{{url '/things/' thing._id '/edit'}}">Edit Thing</a>
  {{else}}
    <p>Thing _id is not defined</p>
  {{/if}}

  <a href="/things/{{thing._id}}">Cancel</a>
</body>

</html>
